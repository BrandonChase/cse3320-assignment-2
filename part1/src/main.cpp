#include <string>
#include <list>

using namespace std;

const string FILENAME = "earthquakes.csv";

int main(int argc, const char * argv[])
{
	//load data as vector of strings
	//time the sorts
	return 0;
}

/*
Returns the number of seconds it takes to sort the data using specified number of processes
num_processes is the number of processes that will handle the sort
num_lines is how much of the data is to be sorted, by default it is whole file unless otherwise
	specified
*/
int timeSort(int num_processes, int num_lines)
{
	//calculate
	//
}

void loadDataIntoVector

void saveDataChunk(string filename, vector<double> content, int start_index, int end_index)
{

}